const settings = require("./settings.controller");
const user = require("./user.controller");
const connections = require("./connections.controller");

module.exports = {
  settings,
  user,
  connections,
};
